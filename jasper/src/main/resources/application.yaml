server:
  port: 4444
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/productdb
    username: root
    password: root
  application:
    name: jasper-service
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
